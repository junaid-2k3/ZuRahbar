# Copy to .env (gitignored) for local emulation, or set these as Cloud Functions v2
# environment variables / .env.<project-id> per Firebase's docs — NOT via the v1
# `firebase functions:config:set` command, which this v2 onRequest function does not read.
DASHSCOPE_API_KEY=
QWEN_API_BASE_URL=
QWEN_MODEL=
